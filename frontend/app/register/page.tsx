"use client"; import Link from "next/link"; import {useRouter} from "next/navigation"; import {useState} from "react"; import {api} from "../../lib/api";
export default function Register(){const r=useRouter(),[displayName,setName]=useState(""),[email,setEmail]=useState(""),[password,setPassword]=useState(""),[error,setError]=useState("");return <main className="auth"><Link href="/">← GROW WORLD</Link><form onSubmit={async e=>{e.preventDefault();try{await api.register({displayName,email,password});r.push("/dashboard")}catch(e){setError(e instanceof Error?e.message:"登録に失敗しました")}}}><p className="eyebrow">START SMALL</p><h1>あなたの土地をつくる</h1><label>表示名<input required maxLength={40} value={displayName} onChange={e=>setName(e.target.value)}/></label><label>メールアドレス<input type="email" required value={email} onChange={e=>setEmail(e.target.value)}/></label><label>パスワード（8文字以上）<input type="password" minLength={8} required value={password} onChange={e=>setPassword(e.target.value)}/></label>{error&&<p className="form-error">{error}</p>}<button>世界をはじめる</button><p>すでに登録済みですか？ <Link href="/login">ログイン</Link></p></form></main>}
