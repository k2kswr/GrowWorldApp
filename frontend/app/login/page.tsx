"use client"; import Link from "next/link"; import {useRouter} from "next/navigation"; import {useState} from "react"; import {api} from "../../lib/api";
export default function Login(){const r=useRouter(),[email,setEmail]=useState(""),[password,setPassword]=useState(""),[error,setError]=useState("");return <main className="auth"><Link href="/">← GROW WORLD</Link><form onSubmit={async e=>{e.preventDefault();try{await api.login({email,password});r.push("/dashboard")}catch(e){setError(e instanceof Error?e.message:"ログインに失敗しました")}}}><p className="eyebrow">WELCOME BACK</p><h1>世界へ戻ろう</h1><label>メールアドレス<input type="email" required value={email} onChange={e=>setEmail(e.target.value)}/></label><label>パスワード<input type="password" required value={password} onChange={e=>setPassword(e.target.value)}/></label>{error&&<p className="form-error">{error}</p>}<button>ログイン</button><p>はじめてですか？ <Link href="/register">新規登録</Link></p></form></main>}
